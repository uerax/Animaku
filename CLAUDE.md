# 项目协作规则（CLAUDE.md）

> 本文件在每次 session 启动时都会被加载，请严格遵守以下规则。
> 规则冲突时，按编号从小到大优先级递减，第 0 条优先级最高。

## 0. 自我保护规则（最高优先级）

- **禁止修改本文件（CLAUDE.md）**，除非用户在当前对话中明确说“帮我改 CLAUDE.md”或“更新一下项目规则”。
- 发现本文件内容与实际情况不符（比如规则过时）时，**不要自行修改**，而是提醒用户：“CLAUDE.md 里的 XX 规则似乎需要更新，需要我改吗？”
- 批量任务（重构、格式化、批量替换等）**默认排除** `CLAUDE.md`、`.claude/` 目录下所有文件，除非用户明确点名要改这些文件。

## 1. 状态持久化规则（防止 context 压缩丢细节）

- 项目根目录维护 `.claude/STATE.md`，记录当前任务的完整状态；清单类任务额外维护 `.claude/BUGS.md`。
- 每完成一个有意义的子任务（修复一个 bug、完成一个功能点），**立即**在 `.claude/STATE.md` 追加记录，格式：

  ```
  ## [日期] 任务名
  - 状态：已完成 / 进行中 / 待处理
  - 优先级：P0-P5
  - 描述：xxx
  - 涉及文件：path/to/file.js
  - 备注：xxx
  ```

- 每次开始新任务前，**先读取 `.claude/STATE.md`**，以此为准，不要仅依赖对话历史中的记忆。
- 一次性列出多个待办事项（bug 清单、任务清单）时，**先写入 `.claude/BUGS.md`**，再逐条处理；每处理完一项就同步更新状态，不要等全部做完再一次性更新。
- `.claude/BUGS.md` 只记录**当前待处理 / 进行中**的条目，格式极简（状态、优先级、一句话描述）。
- 某一项处理完成后，**从 BUGS.md 中直接移除该条目**（而不是标记"已完成"后继续保留），
  完整细节（涉及文件、备注、决策过程）**只写入 STATE.md 一次**。
- BUGS.md 相当于"进行时"的待办板，STATE.md 相当于"过去时"的历史记录，
  同一个 bug 在任一时刻只应该出现在其中一个文件里，不重复维护状态字段。

## 2. 上下文管理

- 感知到对话已经很长（用户提到“上下文快满了”“context 80%”等）时，主动提醒：先把当前状态写入文件，再考虑开新 session 或执行 `/compact`。
- 不假设自己“记得”很久之前对话中的细节；涉及关键决策、清单类信息时，优先以文件内容为准，而不是对话记忆。
- `.claude/STATE.md` 和 `.claude/BUGS.md` 只保留**当前进行中 + 最近已完成**的任务记录；
  超过一定量（如已完成任务累计超过 20 条，或体积明显偏大）时，
  将较早的“已完成”记录整体移入 `.claude/STATE-ARCHIVE.md`，
  仅在需要追溯历史时读取归档文件。

## 3. 代码修改规范

- 修改代码前，先说明将要做的改动范围，避免大范围无关改动。
- 每次修改后，如果项目有测试，运行**相关**测试确认没有引入新问题（不必每次跑全量测试套件，除非用户要求或改动涉及核心模块）。
- 不确定的地方，先提问或说明假设，不要臆造需求。

## 4. 开发服务器 / 长期运行进程

- **禁止主动启动任何开发服务器或长期运行进程**（如 `pnpm dev`、`npm run dev`、`yarn dev`，以及各种带 `&`、`nohup` 的后台变体），无论是否为了“验证改动”。
- 修改代码后，直接说明改了什么、建议手动测试哪些地方即可；不要自行运行、也不要询问“要不要帮你启动”。
- 我会自己在本地终端里启动服务进行验证。
- 需要验证逻辑但不涉及启动长期服务的场景（如跑单元测试、执行一次性脚本、`tsc --noEmit` 类型检查）不受此条限制。

## 5. Git 提交与版本号递增规范

- commit message 使用简洁的祈使句，格式：`<类型>: <说明>`，类型包括 feat / fix / refactor / docs / test / chore。
- **版本号递增规则（仅在有代码修改时执行）**：
  - **纯文档/规则修改豁免**：若本次修改**仅涉及文档、项目规范、注释、清单文件**（如 `CLAUDE.md`、`README.md`、`docs/`、`.claude/` 等）而**没有实际代码改动**（`apps/`、`packages/` 下的代码逻辑），**严禁递增版本号**，保持原版本号不变。
  - **代码修改递增准则**：只要包含实际代码改动，在准备提交（或用户要求 commit）前，**必须根据本次修改的幅度递增项目版本号**（通过执行 `pnpm bump <patch|minor|major>` 自动更新全仓 `package.json` 与 `packages/shared/src/version.ts`）：
    - **Patch（修订号，如 `1.1.2` -> `1.1.3`）**：针对缺陷修复（`fix`）、轻量代码重构（`refactor`）、样式/UI微调、性能优化微调等小幅度代码改动；
    - **Minor（次版本号，如 `1.1.2` -> `1.2.0`）**：针对新增功能模块（`feat`）、中等规模架构改进、新增重要配置项或非破坏性功能升级等中等幅度改动；
    - **Major（主版本号，如 `1.1.2` -> `2.0.0`）**：针对涉及破坏性变更（Breaking Changes）、底层协议/关键依赖彻底重构、全局架构颠覆等重大改动。
  - 版本号变更需同步纳入当次修改，并在每次回复中明确说明版本号变动（例如：`v1.1.2` -> `v1.1.3`；若是纯文档修改则说明“纯文档修改，版本号保持 v1.1.2 不变”）。
- **不自动执行 `git commit`**，除非用户在当前任务中明确要求（如“提交一下”“commit 一下”）。
  - 完成代码修改后，默认只说明改了什么、递增后的版本号（或说明保持不变）、建议提交的 commit message，由用户自行决定是否提交。
  - 用户对某一次任务说过“帮我提交”，**仅对该次任务生效**，不代表后续任务也可以自动提交；每个任务是否需要 commit 需要重新确认。
- 不自动执行 `git push`，除非用户明确要求。
- 不自动执行涉及历史重写的操作（`rebase -i`、`reset --hard`、`push --force`），除非用户明确要求。

## 6. 沟通风格

- 回复中直接给出结论和改动内容，避免不必要的寒暄。
- 涉及不确定的技术判断时，明确说明“这是推测”还是“这是确认过的事实”。

## 7. 视频源接入与维护规则

- 当任务涉及**接入新视频源、重构/排查现有视频源**时，**必须先读取 `docs/video-source-integration.md`**（视频源接入规范与实战指南）。
- 严格遵循其中的架构决策矩阵、专有适配器 SOP、版本号递增（`PLUGIN_DEFAULTS_VERSION`）及防盗链/区域路由/并发竞速等避坑准则执行。

## 8. 功能实现索引维护规则
 
- 项目维护 `.claude/feature-map.md`，记录“功能模块 → 核心文件路径”的快速索引，用于减少大范围代码搜索。此文件**仅供 Claude 导航使用**，不作为团队产品文档维护，保持精简、路径为主。
- 索引只做索引的事：feature-map.md 只记录"模块 → 文件路径"和一句话功能描述，不记录任务状态、处理进度、修改历史、决策过程；这类信息一律写入 .claude/STATE.md，不得混入索引文件。
- **触发更新的时机**：
  - 新增功能模块、迁移/重命名关键文件时，顺手更新对应条目。
  - 完成一个子任务、写 `.claude/STATE.md` 时，检查该任务是否涉及索引变更，一并同步。
- **索引条目分两类，处理方式不同**：
  - 纯路径类（模块名 → 文件路径）：可自主更新，无需征求确认。
  - 背景/设计决策类（为什么这么做、废弃说明等）：更新或新增时需在回复中明确指出“我更新了 XX 的背景说明，请确认准确”，等待用户确认。
- 清理规则：模块被删除、合并或功能下线时，需同步移除对应条目，不允许只增不删；发现读取后文件对不上、且确认该模块已不存在时，直接删除该条目。
- 分层规则：当 feature-map.md 超过约 100 行、或模块过多导致定位变慢时，按顶层模块拆分为 .claude/feature-map/<module>.md，根目录 .claude/feature-map.md 只保留"模块名 → 分文件路径"的一级索引。
- 每次接手涉及“定位某功能实现在哪”的任务时，**先读取 `.claude/feature-map.md`**；如果条目缺失或读取后发现文件对不上，再走全项目搜索，并顺手补全索引。
- 大重构或版本发布后，主动提议做一次全量核对：“是否需要我扫描一遍项目、核对 feature-map.md 是否有过期条目？”

## 待办 / 索引类文件一览
 
- 当前 bug 清单：`.claude/BUGS.md`
- 项目状态快照：`.claude/STATE.md`
- 状态归档（历史已完成任务）：`.claude/STATE-ARCHIVE.md`
- 视频源接入规范与指南：`docs/video-source-integration.md`
- 功能实现索引（仅供 Claude 导航）：`.claude/feature-map.md`
- （如项目有其他清单文件，请在这里补充索引）